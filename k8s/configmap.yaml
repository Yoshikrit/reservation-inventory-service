apiVersion: v1
kind: ConfigMap
metadata:
  name: inventory-config
  namespace: inventory
data:
  REST_PORT: "8080"
  GRPC_PORT: "50051"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  KAFKA_BROKERS: "kafka:9092"
  KAFKA_TOPIC_CONFIRMED_RESERVATION: "reservation-confirmed"
  KAFKA_GROUP_INVENTORY_CONFIRMED_RESERVATION: "inventory-group"
